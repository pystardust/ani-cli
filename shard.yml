name: ani-cli
version: 0.1.0

authors:
  - ani-cli Contributors

targets:
  ani-cli:
    main: src/ani-cli.cr
  libgogo:
    main: src/libgogo.cr

dependencies:
  term-prompt:
    github: crystal-term/prompt
  clim:
    github: at-grandpa/clim
    version: 0.17.1
  term-screen:
    github: crystal-term/screen
# HTTP proxy lib, not currently being used
#  http_proxy:
#    github: mamantoha/http_proxy

crystal: 1.2.2

license: GPLv3