// See https://svelte.dev/docs/kit/types#app for information about these
// interfaces. Empty stubs keep TypeScript happy until we need them.
declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
